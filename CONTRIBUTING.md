# Contributing

- Never commit API keys or other credentials.
- Keep network failures and empty results user-friendly.
- Test category navigation, search, article links, and responsive layouts before submitting changes.
- Keep changes focused and update the README for user-facing behavior.

Prefer descriptive commits such as `feat: add article search`, `fix: handle API failure`, or `docs: clarify environment setup`.
